#!/usr/bin/bash
#
c='\e[0;1;4;38;5;253m'
b='\e[0;38;5;248m'
TIP[0]="${c}cat /proc/cpuinfo ${b}: Show CPU information."
TIP[1]="${c}cat /proc/meminfo ${b}: Check memory usage."
TIP[2]="${c}cat /proc/mounts ${b}: Show the mounted filesystem."
TIP[3]="${c}date ${b}: Display the system date."
TIP[4]="${c}cal 11 2023 ${b}: Show the almanac for the month november 2023."
TIP[5]="${c}tree ${b}: Show files and folders in a tree starting from the root."
TIP[6]="${c}ln -s file1 lnk1 ${b}: Create a symbolic link to the file or directory."
TIP[7]="${c}iconv -l ${b}: Lists of known ciphers."
TIP[8]="${c}find /usr/bin -type f -atime +100 ${b}: Find unused binary files in the last 100 days."
TIP[9]="${c}whereis ls ${b}: show the location of a binary, help, or source file. In this case it asks where the 'ls' command is."
TIP[10]="${c}which ls ${b}: show the full path to a binary/executable."
TIP[11]="${c}df -h ${b}: Show a list of mounted partitions."
TIP[12]="${c}ls -lSr |more ${b}: show the size of files and directories ordered by size."
TIP[13]="${c}du -sh dir1 ${b}: Estimate the space used by directory 'dir1'."
TIP[14]="${c}du -sk * | sort -rn ${b}: show the size of files and directories sorted by size."
TIP[15]="${c}ls -a ~home | pr -T5 -W\$COLUMNS ${b}: split the terminal into 5 columns."
TIP[16]="${c}chmod ugo+rwx directory1 ${b}: set read ®, write (w) and execute (x) permissions to the owner (u), the group (g) and others (o) on the directory 'directory1'."
TIP[17]="${c}gzip -9 file1 ${b}: compress with maximum compression"
TIP[18]="${c}tar -cvf archive.tar file1 file2 dir1 ${b}: create an archive containing 'file1', 'file2′ and 'dir1'."
TIP[19]="${c}rpm -ivh package.rpm ${b}: Install an rpm package."
TIP[20]="${c}rpm -qa ${b}: Show all rpm packages installed on the system."
TIP[21]="${c}rpm -q package_name –scripts ${b}: Show scripts started during installation/removal."
TIP[22]="${c}dpkg -l ${b}: Show all deb packages installed on the system."
TIP[23]="${c}tac file1 ${b}: view the contents of a file starting from the last line."
TIP[24]="${c}less file1 ${b}: similar to the 'more' command but allows saving the movement in the file as well as the movement backwards."
#text manipulation
TIP[25]="${c}cat file1 | command( sed, grep, awk, grep, etc…) > result.txt ${b}: general syntax to manipulate text in a file and write the result to a new file."
TIP[26]="${c}cat file1 | command( sed, grep, awk, grep, etc…) » result.txt ${b}: general syntax to manipulate text in a file and add result to an existing file."
TIP[27]="${c}grep ^Aug /var/log/messages ${b}: search for words starting with “Aug” in file '/var/log/messages'"
TIP[28]="${c}sed 's/stringa1/stringa2/g' example.txt ${b}: relocate “string1” with “string2” in example.txt"
TIP[29]="${c}sed '/ *#/d; /^$/d' example.txt ${b}: remove comments and blank lines from example.txt"
TIP[30]="${c}echo 'esempio' | tr '[:lower:]' '[:upper:]' ${b}: Convert lowercase to uppercase."
#Set character and file conversion
TIP[31]="${c}dos2unix filedos.txt fileunix.txt ${b}: convert a text file format from MSDOS to UNIX."
TIP[32]="${c}unix2dos fileunix.txt filedos.txt ${b}: convert a text file format from UNIX to MSDOS."
TIP[33]="${c}recode .. HTML < page.txt > page.html ${b}: convert a text file into html."
TIP[34]="${c}recode -l | more ${b}: Show all available format conversions."
#Format a file system
TIP[35]="${c}mkfs /dev/hda1 ${b}: create a Linux-type system file on the hda1 partition."
TIP[36]="${c}mkfs -t vfat 32 -F /dev/hda1 ${b}: create a FAT32 system file on hda1."
#Saves (Backup)
TIP[37]="${c}rsync -rogpav –delete /home /tmp ${b}: Synchronization between directories."
TIP[38]="${c}rsync -rogpav -e ssh –delete /home ip_address:/tmp ${b}: rsync over the SSH tunnel."
TIP[39]="${c}rsync -az -e ssh –delete ip_addr:/home/public /home/local ${b}: Synchronize a local directory with a remote directory via ssh and compression."
TIP[40]="${c}dd bs=1M if=/dev/hda | gzip | ssh user@ip_addr 'dd of=hda.gz' ${b}: save a hard drive on a remote host via ssh."
TIP[41]="${c}dd if=/dev/sda of=/tmp/file1 ${b}: save the contents of a hard drive to a file. (In this case the hard drive is “sda” and the file “file1”)."
TIP[42]="${c}tar -Puf backup.tar /home/user ${b}: make an incremental backup of the '/home/user' directory."
TIP[43]="${c}( cd /tmp/local/ && tar c . ) | ssh -C user@ip_addr 'cd /home/share/ && tar x -p' ${b}: Copy the contents of a directory to a remote directory via ssh."
TIP[44]="${c}( tar c /home ) | ssh -C user@ip_addr 'cd /home/backup-home && tar x -p' ${b}: Copy a local directory to a remote directory via ssh."
TIP[45]="${c}find /home/user1 -name '*.txt' | xargs cp -av –target-directory=/home/backup/ –parents ${b}: find and copy all files with '.txt' extension from one directory to another."
TIP[46]="${c}find /var/log -name '*.log' | tar cv –files-from=- | bzip2 > log.tar.bz2 ${b}: find all files with '.log' extension and make a bzip archive."
TIP[47]="${c}dd if=/dev/hda of=/dev/fd0 bs=512 count=1 ${b}: make a copy of the MRB (Master Boot Record) to a floppy disk."
#Work with the NETWORK ( LAN and Wi-Fi)
TIP[48]="${c}ifconfig eth0 ${b}: Display the configuration of an Ethernet network card."
TIP[49]="${c}ifconfig eth0 192.168.1.1 netmask 255.255.255.0 ${b}: Configure an IP address."
TIP[50]="${c}route add -net 0/0 gw IP_Gateway ${b}: configure default gateway."
TIP[51]="${c}echo “1” > /proc/sys/net/ipv4/ip_forward ${b}: activate ip traversal."
TIP[52]="${c}hostname ${b}: Display the hostname of the system."
TIP[53]="${c}host www.example.com ${b}: Find the host name to resolve the name to an ip(1) address."
TIP[54]="${c}ip link show ${b}: Show the link status of all interfaces."
TIP[55]="${c}netstat -tup ${b}: Show all active network connections and their PIDs."
TIP[56]="${c}iwlist scan ${b}: show wireless networks."
TIP[57]="${c}iwconfig eth1 ${b}: Display the configuration of a wireless network card."
#IP Tables (FIREWALL)
TIP[58]="${c}iptables -t filter -L ${b}: Show all strings in the filter table."
TIP[59]="${c}iptables -t nat -L ${b}: Show all strings from the nat table."
TIP[60]="${c}iptables -t filter -F ${b}: Clear all filter table rules."
TIP[61]="${c}iptables -t filter -A INPUT -p tcp –dport telnet -j ACCEPT ${b}: Allow telnet connections to enter."
TIP[62]="${c}iptables -t filter -A OUTPUT -p tcp –dport http -j DROP ${b}: Block HTTP connections from exiting."
TIP[63]="${c}iptables -t filter -A FORWARD -p tcp –dport pop3 -j ACCEPT ${b}: Allow POP connections to a forward chain."
TIP[64]="${c}iptables -t nat -A PREROUTING -d 192.168.0.1 -p tcp -m tcp –dport 22 -j DNAT –to-destination 10.0.0.2:22 ${b}: Redirect packets directed from one host to another."
#Monitoring and debugging
TIP[65]="${c}top ${b}: show linux tasks using most cpu."
TIP[66]="${c}ps -eafw ${b}: Shows Linux tasks."
TIP[67]="${c}pstree ${b}: display a system tree of processes."
TIP[68]="${c}kill -9 ID_Processo ${b}: force a process to close and terminate it."
TIP[69]="${c}lsof -p \$$ ${b}: show a list of files opened by processes."
TIP[70]="${c}lsof /home/user1 ${b}: shows a list of files open in a given path on the system."
TIP[71]="${c}strace -c ls >/dev/null ${b}: Display system calls made and received by a process."
TIP[72]="${c}strace -f -e open ls >/dev/null ${b}: Show library calls."
TIP[73]="${c}watch -n1 'cat /proc/interrupts' ${b}: Show interruptions in real time."
TIP[74]="${c}free -m ${b}: Displays the RAM status in megabytes."
#Other useful commands
TIP[75]="${c}apropos …keyword ${b}: mostrar una lista de comandos que pertenecen a las palabras claves de un programa; son útiles cuando tú sabes qué hace tu programa, pero de sconoces el nombre del comando."
TIP[76]="${c}man ping ${b}: show online man pages; for example, in a ping command, use the '-k' option to find any related commands."
TIP[77]="${c}whatis …keyword ${b}: shows the description of what the program does."
TIP[78]="${c}gpg -c file1 ${b}: encode a file with GNU security guard."
TIP[79]="${c}gpg file1.gpg ${b}: decode a file with GNU Security Guard."
TIP[80]="${c}wget -r www.example.com ${b}: Download an entire website."
TIP[81]="${c}wget -c www.example.com/file.iso ${b}: download a file with the ability to stop downloading and resume later."
TIP[82]="${c}echo 'wget -c www.example.com/files.iso ' | at 09:00 ${b}: Start a download at any time. In this case it would start at 9 am."
TIP[83]="${c}ldd /usr/bin/ssh ${b}: Display the shared libraries required by the ssh program."
TIP[84]="${c}alias hh='history' ${b}: set an alias for a command –hh= History."

TIP[85]="${c}lsof ${b}: lists files opened by running processes."
TIP[86]="${c}diff file_1 file_2 ${b}: Command to find differences between files."


Random=$[ $RANDOM % 84 ]
tips="\e[1;95mTIP:\e[0;38;5;251m Run ${TIP[$Random]}\e[0m\n"
#echo -en $tips 
